function rectangleAttack(spec) {
	var type = shape.type,
	width = shape.width,
	height = shape.height,
	immune = spec.immune || [],
	damage = spec.damage,
	rotation = spec.rotation || 0,
	mask = spec.mask,
	division = spec.division || 1;




}
