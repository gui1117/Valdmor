function createGrid() {
	var smallSquare = spec.smallSquare,
	bigSquare = spec.bigSquare,
	width = spec.width,
	height = spec.height,
	
	grid,
	WALL = 0,
	SOUND = 1,

	addSound = function(spec) {
	},
	getSound = function(p) {
	},
	updateSound = function() {
	},
	update = function(dt) {
	},
	draw = function() {
	},
	setWall = function(wallGrid) {
	},
//	toGrid = function(pos) {
//	},
//	toWorld = function(pos,center) {
//	},
//	findPath = function(from,to) {
//	};
}
